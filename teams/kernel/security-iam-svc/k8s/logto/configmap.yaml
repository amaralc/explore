apiVersion: v1
kind: ConfigMap
metadata:
  name: logto-config
  namespace: logto
  labels:
    app.kubernetes.io/name: logto
    app.kubernetes.io/part-of: security-iam-svc
data:
  TRUST_PROXY_HEADER: "1"
  # ENDPOINT and ADMIN_ENDPOINT are set via environment-specific configuration
  # in the Terraform orchestrator (security-iam-svc/iac/main.tf)
