// import executor from './executor';
// import { BrowserExecutorSchema } from './schema';

// const options: BrowserExecutorSchema = {};

describe('Browser Executor', () => {
  it('can run', async () => {
    // const output = await executor(options);
    // expect(output.success).toBe(true);
  });
});
